{
  "name": "echo_test",
  "display_name": "Echo Test (throwaway)",
  "version": "0.1.0",
  "description": "Not a real plugin — exists purely to exercise the plugin mechanism end-to-end (Pen Test tab, authorization gate, form fields, extra_params passthrough, agent dispatch). Safe to uninstall once you've confirmed everything works.",
  "job_types": [
    {
      "type": "echo_test_scan",
      "label": "Echo Test",
      "agent_capability": "echo_test_scan",
      "risk_tier": "high",
      "tab": "pentest",
      "section": "network",
      "form_fields": [
        { "name": "target",  "type": "text", "required": true,  "label": "Target" },
        { "name": "message", "type": "text", "required": false, "label": "Test message" }
      ]
    }
  ],
  "hooks": []
}
